@model IEnumerable<DoAnCoSo.Models.CartItem>

@{
    ViewBag.Title = "Cart";
    Layout = "~/Views/Shared/_Layout.cshtml";
}







@*<h2 style="text-align:center">THÔNG TIN GIỎ HÀNG</h2>
<table align="center" border="1">
    <tr style="text-align:center; font-weight:bold">
        <td> Tên sản phẩm </td>
        <td> Ảnh sản phẩm </td>
        <td> Số lượng </td>
        <td> Đơn giá </td>
        <td> Thành tiền </td>
        <td width="50px"></td>
        <td width="50px"></td>
        <td width="50px"></td>
    </tr>
    @foreach (var item in Model)
    {
        <tr style="text-align:center; font-weight:bold">
            <td> @item.title </td>
            <td><img src="~/Content/Images/@item.image" style="width:200px" /> </td>
            @using (Html.BeginForm("UpdateCart", "ShoppingCart", new { id = item.id }))
            {
                <td>
                    <input type="number" min="1" name="txtQuantity" value="@item.quantity" />
                </td>
                <td> @String.Format("{0:0,0}", item.price) </td>
                <td> @String.Format("{0:0,0}", item.Money) </td>
                <td><a style="color:black" href="@Url.Action("Details", "Book", new { id = item.id })"> Chi tiết </a></td>
                <td><a style="color:black" href="@Url.Action("Remove", "Book", new { id = item.id })"> Xoá </a></td>
                <td> <input style="color:black" type="submit" value="Cập nhật" /></td>
            }
        </tr>

    }
   
    <tr style="font-weight:bold; text-align:right; color:black">
        <td colspan="5"> Số lượng sản phẩm: @ViewBag.Tongsoluong </td>
        <td colspan="5"> Tổng tiền: @String.Format("{0:0,0}", ViewBag.Tongtien) VNĐ</td> 
    </tr>
    <tr style="font-weight:bold; color:red; text-align:right">
        <td colspan="9">
                
        </td>
    </tr>
    <tr style="font-weight: bold; text-align: right;color:red">
        <td colspan="5">
            @Html.ActionLink("Thanh toán sau khi nhận", "Order", "ShoppingCart")
        </td>
        <td colspan="5">
            @Html.ActionLink("Thanh toán MOMO", "Payment", "ShoppingCart")
        </td>
    </tr>
</table>*@




<section class="h-100" style="background-color: #eee;">
    <div class="container h-100 py-5">
        <div class="row d-flex justify-content-center align-items-center h-100">
            <div class="col-10">

                <div class="d-flex justify-content-between align-items-center mb-4">
                    <h3 class="fw-normal mb-0 text-black">Giỏ hàng</h3>
                    
                </div>
                @foreach (var item in Model)
                {
                    <div class="card rounded-3 mb-4">
                        <div class="card-body p-4">
                            <div class="row d-flex justify-content-between align-items-center">
                                <div class="col-md-2 col-lg-2 col-xl-2">
                                    <img src="~/Content/Images/@item.image" class="img-fluid rounded-3" alt="Cotton T-shirt" />

                                </div>
                                <div class="col-md-3 col-lg-3 col-xl-3">
                                    <p class="lead fw-normal mb-2">@item.title</p>

                                </div>
                                <div class="col-md-3 col-lg-3 col-xl-2 d-flex">
                                    @using (Html.BeginForm("UpdateCart", "ShoppingCart", new { id = item.id }))
                                    {
                                        <input type="number" min="1" name="txtQuantity" value="@item.quantity" class="form-control form-control-sm" />
                                    }
                                </div>
                                <div class="col-md-3 col-lg-2 col-xl-2 offset-lg-1">
                                    <h6 class="mb-0" style="width: 150px;"> @item.FormattedCartItem</h6>
                                </div>
                                <div class="col-md-1 col-lg-1 col-xl-1 text-end">
                                    <a href="/ShoppingCart/RemoveCartItem/@item.id" class="text-danger"><i class="fa fa-trash"></i></a>
                                    @*@Html.ActionLink("Xóa Giỏ Hàng", "Delete")*@
                                    @*<a style="color:black" href="@Url.Action("RemoveCartItem", "ShoppingCart", new { id = item.id })"> Xoá </a>*@
                                </div>
                            </div>
                        </div>
                    </div>
                }


                @*<div class="card mb-4">
            <div class="card-body p-4 d-flex flex-row">
                <div class="form-outline flex-fill">
                    <input type="text" id="form1" class="form-control form-control-lg" />
                    <label class="form-label" for="form1">Discound code</label>
                </div>
                <button type="button" class="btn btn-outline-warning btn-lg ms-3">Apply</button>
            </div>
        </div>*@
                

            <div class="card">
              
                <div class="card-body">
                    @*Số lượng sản phẩm: @ViewBag.Tongsoluong*@ 
                    Tổng tiền: @String.Format("{0:0,0}", ViewBag.Tongtien) VNĐ
                    <a href="/ShoppingCart/Order" style="float:right" ; class="btn btn-primary btn-lg active" role="button" aria-pressed="true">Thanh toán sau khi nhận</a>
                    <a href="/ShoppingCart/Payment" style="float:right" ; class="btn btn-secondary btn-lg active" role="button" aria-pressed="true">Thanh toán MoMo</a>
                </div>
            </div>

            </div>
        </div>
    </div>
</section>

